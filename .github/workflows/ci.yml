name: PRISM CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  supply-chain:
    name: Licence firewall + manifest + SBOM
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Licence firewall (no copyleft in linked components)
        run: python scripts/licence_check.py
      - name: Linked libraries match third_party/MANIFEST.toml
        run: python scripts/fetch_deps.py --linked
      - name: Linked libraries match the pinned upstream archives (sha256)
        run: python scripts/fetch_deps.py --linked --refetch
      - name: CycloneDX SBOM
        run: python scripts/sbom.py --version "${GITHUB_SHA::12}" -o prism.cdx.json
      - uses: actions/upload-artifact@v4
        with:
          name: prism-sbom
          path: prism.cdx.json
          retention-days: 30

  cpp:
    name: C++ engine (build + prism_tests)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install toolchain
        run: sudo apt-get update && sudo apt-get install -y ninja-build clang ccache qt6-base-dev libgl-dev libclang-18-dev
      - uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ccache-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'third_party/z3/CMakeLists.txt') }}-${{ github.sha }}
          restore-keys: ccache-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'third_party/z3/CMakeLists.txt') }}-
      - name: Configure
        run: >
          cmake -B build -G Ninja
          -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++
          -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          -DPRISM_CUDA=OFF -DPRISM_LLAMA=OFF -DPRISM_QT=ON -DPRISM_Z3=ON
      - name: Build
        run: cmake --build build
      - name: prism_tests
        run: ./build/prism_tests
      - name: SV-COMP archive packager smoke
        run: python3 tools/svcomp/package_archive.py --prism build/prism --out /tmp/prism-svcomp.tar.gz
      - name: GUI smoke (headless Qt, real pipeline run through the window)
        run: |
          mkdir -p gui-smoke/src
          cp testdata/add_overflow.c testdata/div_param.c testdata/oob_write.c gui-smoke/src/
          QT_QPA_PLATFORM=offscreen timeout 600 ./build/prism_gui gui-smoke/src --no-llm \
            --stage inventory,classify,lints,bmc --out gui-smoke/out \
            --smoke-screenshot gui-smoke/prism_gui.png
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: prism-gui-smoke
          path: gui-smoke/prism_gui.png
          retention-days: 7
      - uses: actions/upload-artifact@v4
        with:
          name: prism-linux
          path: |
            build/prism
            build/libprism_native.so
          retention-days: 7

  python:
    name: Python engine (pytest) + engine parity
    runs-on: ubuntu-24.04
    needs: cpp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test deps and the linters the polyglot stage drives
        run: |
          sudo apt-get update && sudo apt-get install -y shellcheck clang
          python -m pip install --upgrade pip
          python -m pip install pytest z3-solver ruff mypy yamllint PySide6-Essentials
          sudo apt-get install -y libegl1 libxkbcommon0
      - uses: actions/download-artifact@v4
        with:
          name: prism-linux
          path: build
      - name: pytest
        env:
          PRISM_BIN: ${{ github.workspace }}/build/prism
          QT_QPA_PLATFORM: offscreen
        run: |
          chmod +x build/prism
          python -m pytest -q tests

  selfcheck:
    name: PRISM on PRISM (SARIF artifact, not gating)
    runs-on: ubuntu-24.04
    needs: cpp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: prism-linux
          path: build
      - name: Polyglot + lints over the repo
        run: |
          chmod +x build/prism
          ./build/prism . --no-llm --stage inventory,lints,polyglot --out prism-out-self || true
      - uses: actions/upload-artifact@v4
        with:
          name: prism-self-report
          path: |
            prism-out-self/report.md
            prism-out-self/report.sarif
          retention-days: 14
